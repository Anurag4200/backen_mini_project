<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white space-y-4">
      <h1 class="text-3xl tracking-tighter">Create A Post</h1>
      <div class="w-10 h-10 rounded-md bg-red-500 overflow-hidden">
        <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profile%>" alt="img">
        
      </div>
      <h1 class="text-3xl tracking-tighter capitalize">Welcome, <%=user.name%></h1>
      <form action="/post/create" method="post">
        <textarea
          name="content"
          class="resize-none w-1/3 bg-zinc-700 rounded-md outline-none px-3 py-2"
        ></textarea>
        <input
          class="px-3 py-2 cursor-pointer bg-blue-500 hover:bg-blue-600 rounded-md block"
          type="submit"
          value="Create Post"
        />
      </form>
      <div class="space-y-3">
        <h5 class="text-xl text-zinc-500 mt-10">Your Post</h5>
        <%post.forEach((post)=>{%>
            <div class="w-1/3 bg-zinc-800 border border-zinc-700 px-3 py-2 space-y-3">
                <h5 class="text-sm text-blue-500"><%=user.name%></h5>
                <p><%=post.postData%></p>
                <h3 class="text-sm text-zinc-400">Likes <%=post.likes.length%></h3>
                <div class="flex gap-3">
                  <a class="text-blue-500 hover:text-blue-600" href="/like/<%=post._id%>"><%=post.likes.length>0? "Unlike":"Like"%></a>
                  <a class="text-yellow-500 hover:text-yellow-600" href="/edit/<%=post._id%>">Edit</a>
                  <a class="text-red-500 hover:text-red-600" href="/delete/<%=post._id%>">Delete</a>
                </div>
              </div>
        <%})%>
      </div>
    </div>
  </body>
</html>
